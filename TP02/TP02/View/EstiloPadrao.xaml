<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TP02.View.EstiloPadrao">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="SHIFT - Xamarin"
                   TextColor="Green"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center" />
            <Label Text="Aplicativo Cross-Platform"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center"
                   TextColor="Green"
                   />

            <Entry Text="Estilo Baseado no baseStyle" BackgroundColor="Cyan"
                   VerticalOptions="Center"
                   HorizontalOptions="CenterAndExpand"/>
            <Button Text="Clique Aqui" TextColor="White" BackgroundColor="Blue"
                    VerticalOptions="Center"
                    HorizontalOptions="CenterAndExpand"/>
            <Button Text="SEM ESTILO"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>